<div class="flex w-full flex-col h-[calc(100vh-2rem)]">
  <main class="flex flex-col gap-2 p-4 md:gap-3 md:p-4 h-full">
    <!-- Database Error Alert -->
    <% if @db_error %>
      <div class="bg-yellow-50 border border-yellow-200 rounded-lg p-4 mb-4">
        <div class="flex">
          <div class="flex-shrink-0">
            <svg class="h-5 w-5 text-yellow-400" viewBox="0 0 20 20" fill="currentColor">
              <path fill-rule="evenodd" d="M8.257 3.099c.765-1.36 2.722-1.36 3.486 0l5.58 9.92c.75 1.334-.213 2.98-1.742 2.98H4.42c-1.53 0-2.493-1.646-1.743-2.98l5.58-9.92zM11 13a1 1 0 11-2 0 1 1 0 012 0zm-1-8a1 1 0 00-1 1v3a1 1 0 002 0V6a1 1 0 00-1-1z" clip-rule="evenodd" />
            </svg>
          </div>
          <div class="ml-3">
            <h3 class="text-sm font-medium text-yellow-800">
              Base de datos no conectada
            </h3>
            <div class="mt-2 text-sm text-yellow-700">
              <p>
                La aplicación está funcionando pero temporalmente sin conexión a la base de datos. 
                Las funciones que requieren datos estarán deshabilitadas hasta configurar la base de datos.
              </p>
            </div>
          </div>
        </div>
      </div>
    <% end %>

    <!-- Dashboard Header -->
    <div class="flex items-center gap-4 mb-2">
      <h1 class="text-2xl font-semibold gradient-text">Página principal</h1>
    </div>

    <!-- Main Dashboard Cards -->
    <div class="dashboard-grid gap-2">
      <%= render 'dashboard/stock_card' %>
      <%= render 'dashboard/projects_card' %>
      <%= render 'dashboard/budget_card' %>
    </div>

    <!-- Secondary Dashboard Section -->
    <div class="dashboard-section mt-1 flex-1 min-h-0">
      <!-- Recent Projects Section -->
      <div class="dashboard-projects-section h-full">
        <%= render 'dashboard/recent_projects' %>
      </div>

      <!-- Side Cards -->
      <div class="grid auto-rows-max items-start gap-2">
        <%= render 'dashboard/prices_card' %>
      </div>
    </div>
  </main>
</div>